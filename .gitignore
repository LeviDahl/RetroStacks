# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Xcode
build/
!api/build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Scripts/leak-check.sh output (traces + logs, regenerated per run)
/leak-results/

# Swift Package Manager
.build/
.swiftpm/
Packages/
# Package.resolved is intentionally committed for app projects

# CocoaPods / Carthage (not used, but ignore if added later)
Pods/
Carthage/Build/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Secrets — never commit tokens / keys / provisioning
.env
.env.*
*.xcconfig
Secrets.swift
Secrets.plist
*.p12
*.pem
*.key
*.mobileprovision
*.cer

# Injection / misc
*.orig
*~

# api build output (published by CI)
api/dist/

# Point-in-time analysis reports — derived, go stale immediately as the live
# catalog changes, not source data.
api/data/pricecharting-match-*.json
api/data/pricecharting-region-check-*.json
api/data/rgc-import-report.json

# Instruments traces (multi-GB, local diagnostics only)
traces/
